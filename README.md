## 📚 Library API

This project is a **Library Management API** built with **Spring Boot** and **Spring Data JPA**, designed to manage authors and books in a relational database.  
It demonstrates best practices in **entity relationships**, **JPA mappings**, **transaction management**, and **integration with PostgreSQL** using **Docker**.  
The project also includes **repository tests** to validate data persistence and relationships between entities.

---

## 📚 API de Biblioteca

Este projeto é uma **API de Gerenciamento de Biblioteca** desenvolvida com **Spring Boot** e **Spring Data JPA**, voltada para o gerenciamento de autores e livros em um banco de dados relacional.  
Ele aplica boas práticas de **mapeamento de entidades**, **relacionamentos JPA**, **controle transacional** e **integração com PostgreSQL** utilizando **Docker**.  
O projeto também conta com **testes de repositório** para validar a persistência e os relacionamentos entre as entidades.
